[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "comp"
version = "0.1.0a0"
description = "Authority-first evidence compiler core"
readme = "README.md"
requires-python = ">=3.11"

[tool.setuptools]
packages = [
  "comp",
  "comp.judgment",
]
